name: Skyramp Testbot

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.head.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  testbot:
    runs-on: ubuntu-latest
    if: >-
      github.event_name != 'issue_comment' ||
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
    permissions:
      contents: write
      pull-requests: write
    steps:
      # NOTE: testbot v0.10.x action inputs are camelCase. test_directory is NOT an input
      # (it comes from .skyramp/workspace.yml). SUT = Immich server built from source on :2285.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: skyramp/testbot@56fac3cc7b777cd08598ed7a7586f8d9733f3507 # v0.11.11
        with:
          uiCredentials: 'admin@immich.test:EvalPass123!'
          skyrampMcpVersion: "0.4.1-rc.1"
          skyrampExecutorVersion: "v1.3.44"
          skyrampLicenseFile: ${{ secrets.SKYRAMP_LICENSE_FILE }}
          anthropicApiKey: ${{ secrets.SKYRAMP_TESTBOT_API_KEY }}
          targetSetupCommand: './deployment-testbot/setup.sh'
          targetReadyCheckCommand: 'curl -f http://localhost:2285/api/server/ping'
          targetReadyCheckTimeout: '1800'
          authTokenCommand: 'bash get-auth-token.sh'
          autoCommit: true
          postPrComment: true
